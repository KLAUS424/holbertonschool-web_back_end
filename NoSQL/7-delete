// 7-delete

// Delete all documents with name "Holberton school"
// Use deleteMany() or remove() and explicitly print JSON result
var result = db.school.deleteMany({ name: "Holberton school" });

// Explicitly print acknowledged result to stdout
print('{ "acknowledged" : ' + result.acknowledged + ', "deletedCount" : ' + result.deletedCount + ' }');
