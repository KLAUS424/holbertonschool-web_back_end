// 7-delete

// Delete all documents with name "Holberton school"
// Use "delete()" to ensure WriteResult is returned for MongoDB v3.6 tests
var result = db.school.delete({ name: "Holberton school" });

// Print the result so the test can capture it
printjson(result);
