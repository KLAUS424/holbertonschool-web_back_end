// 7-delete
// Deletes all documents in the 'school' collection where the 'name' field is "Holberton school".
// The deleteMany() method is used to return the expected result object { acknowledged: true, deletedCount: N }.
var result = db.school.deleteMany({ name: "Holberton school" });

// Prints the result object to stdout.
print(result);
