// 7-delete

// Remove all documents where name is "Holberton school"
// Using remove() instead of deleteMany() to ensure WriteResult is returned
var result = db.school.remove({ name: "Holberton school" });

// Print the result so test can capture it
printjson(result);
