// 6-update

var result = db.school.updateMany(
  { name: "Holberton school" },        // filter
  { $set: { address: "972 Mission street" } },  // update
  { upsert: false }                     // do not insert if no match
);

printjson(result);  // <--- add this to actually print WriteResult

